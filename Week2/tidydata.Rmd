---
title: "hw1"
author: "陳樞元"
date: "2019/7/11"
output: html_document
---
# Q:當地犯罪數是否與人口有關聯?
###### [桃園市刑事案件分類發生數、破獲數及嫌疑犯人數](https://data.gov.tw/dataset/25883)

###### [桃園市政府警察局轄區現住人口、性別比例及人口密度](https://data.gov.tw/dataset/26053)

# 載入套件
```{r}
library(readr)
library(dplyr)
library(reshape)
```
# 載入桃園刑事案件數
```{r}
a <- read_csv("桃園市刑事案件分類發生數、破獲數及嫌疑犯人數.csv")
```
# 取刑事平均數
```{r}
crime <- a %>% 
group_by(breau) %>%
  summarise(avg_h_count = mean(h_count))
```
# 載入各區人口密度
```{r}
people <- read_csv("桃園市政府警察局轄區現住人口、性別比例及人口密度.csv")
```
# 取人口密度平均數並整理
```{r}
popular <- people %>%
group_by(breau) %>%
  summarise(avg_density = mean(density))

rpopular <- popular[-6,]
```
# 結合並整理資料
```{r}
combine <- merge(crime, rpopular, all = FALSE)
final <- select(combine, breau, avg_density, avg_h_count)
```
# 修改欄目名稱
```{r}
rename(final,c(breau = "轄區",avg_density = "犯罪次數",avg_h_count = "人口密度"))
```